/**
 * Header - Eventos específicos del header
 * Usa el AuthManager centralizado para toda la autenticación
 */

document.addEventListener("DOMContentLoaded", function () {
  // El AuthManager ya se inicializa automáticamente
  // Solo agregamos funcionalidad adicional específica del header aquí
  
  console.log("✅ Header inicializado con AuthManager centralizado");
});
